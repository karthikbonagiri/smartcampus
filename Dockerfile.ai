FROM python:3.10-slim
WORKDIR /app
COPY apps/ai-service/requirements.txt .
RUN pip install -r requirements.txt
COPY apps/ai-service/app ./app
ENV PYTHONPATH=/app
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]// Placeholder: Dockerfile.ai
